{
  "buildCommand": "npm run build && rm -rf node_modules/mongodb-client-encryption node_modules/@mongodb-js/zstd node_modules/kerberos node_modules/snappy && npm prune --omit=dev",
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
